services:
  - type: web
    name: eurusd-strategia
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run eurusd.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
    plan: free
